import Foundation

struct Constants {
    static let usersCollection = "users"
    static let eventsCollection = "events"
    static let matchesCollection = "matches"
    static let defaultSearchRadius: Double = 5.0 // kilometers
}
